# Survey 专区

这里放专题综述型笔记。它和 `arxiv_daily_papers/` 的区别是：

- `arxiv_daily_papers/`：每天筛论文，偏研究雷达。
- `surveys/`：围绕一个主题沉淀学习路线、论文谱系、核心机制和后续深读计划。

每个专题建议一个子目录：

```text
reading/surveys/topic_name/
```

推荐包含：

- `index.tex`：专题入口。
- `survey_YYYY_YYYY.tex`：主综述。
- `README.md`：维护说明。
- `paper_notes/`：可选，后续单篇深读。

